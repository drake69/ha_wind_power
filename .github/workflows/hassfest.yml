name: Hassfest

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  hassfest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: home-assistant/actions/hassfest@master
